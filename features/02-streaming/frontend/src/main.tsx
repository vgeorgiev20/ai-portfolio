import React from "react";
import ReactDOM from "react-dom/client";
import StreamingChat from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <StreamingChat />
  </React.StrictMode>
);
